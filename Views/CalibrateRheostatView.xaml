<UserControl x:Class="BlueEyes.Views.CalibrateRheostatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BlueEyes.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <StackPanel Orientation="Vertical">
        <ComboBox ItemsSource="{Binding Source={StaticResource ConnectedPeripherals}}"
                  SelectedItem="{Binding Path=SelectedPeripheral}"
                  SelectedIndex="0"/>
        <StackPanel Orientation="Horizontal" Margin="0,20" HorizontalAlignment="Center" Height="24">
            <TextBlock Text="Value:"/>
            <TextBox Width="80">
                <TextBox.InputBindings>
                    <KeyBinding Command="{Binding SendValueCommand}" Key="Return" />
                </TextBox.InputBindings>
            </TextBox>
            <StackPanel Orientation="Vertical">
                <Button Content="▲" FontSize="6"/>
                <Button Content="▼" FontSize="6"/>
            </StackPanel>
        </StackPanel>
        <Button Content="Finish" HorizontalAlignment="Center" Height="24" Padding="10,2"/>
    </StackPanel>
</UserControl>
