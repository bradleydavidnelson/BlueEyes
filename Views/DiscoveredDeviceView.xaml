<UserControl x:Class="BlueEyes.Views.DiscoveredDeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BlueEyes.Views"
             mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="discoveredDevicesTemplate">
            <DockPanel DockPanel.Dock="Top" Margin="4,2">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{Binding Path=Name}"/>
                    <TextBlock Text="{Binding Path=AddressString}" FontSize="10" />
                </StackPanel>
                <Button DockPanel.Dock="Right" Content="{Binding Path=ConnectAction}" Width="80" HorizontalAlignment="Right" Command="{Binding Path=ConnectCommand}"/>
            </DockPanel>
        </DataTemplate>
    </UserControl.Resources>
    
    <DockPanel>
        <ItemsControl ItemsSource="{Binding DiscoveredDevices}" ItemTemplate="{StaticResource discoveredDevicesTemplate}"/>
    </DockPanel>
</UserControl>
