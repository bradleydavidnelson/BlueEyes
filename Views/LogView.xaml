<UserControl x:Class="BlueEyes.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BlueEyes.Views"
             xmlns:utilities="clr-namespace:BlueEyes.Utilities"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <ScrollViewer x:Name="scrollViewer" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl Background="Black" ItemsSource="{Binding Document, NotifyOnTargetUpdated=True, Mode=TwoWay}" TargetUpdated="ItemsControl_SourceUpdated"/><!--TODO this doesn't work after you scroll-->
        </ScrollViewer>
    </Grid>
</UserControl>
